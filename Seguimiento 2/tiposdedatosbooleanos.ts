// Declaración de variables booleanas
let estabien: boolean = false;
let tieneunError: boolean = true;

// Asignación de valores booleanos
estabien = true;
tieneunError = false;

// Uso de operadores booleanos
const canAccess: boolean = estabien && !tieneunError;

console.log(canAccess);